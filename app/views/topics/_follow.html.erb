<%= form_for follow_topic, remote: true do |f| %>
  <%= hidden_field_tag :topic_id, topic.id %>

  <p>
    <%= topic.user_topics.size %>
    <%= t ".followers" %>
  </p>

  <%= button_tag t(".follow"), class: "follow" %>
<% end %>
